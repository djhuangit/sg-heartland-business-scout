# Required: Google Gemini API key
GEMINI_API_KEY=your-gemini-api-key

# Optional: data.gov.sg API key for higher rate limits
# DATAGOV_API_KEY=your-datagov-api-key

# Optional: PostgreSQL (run `docker compose up -d` to start)
# If unset, the app uses an in-memory knowledge base
# DATABASE_URL=postgresql+asyncpg://scout:scout@localhost:5432/scout
# DATABASE_URL_SYNC=postgresql+psycopg2://scout:scout@localhost:5432/scout
